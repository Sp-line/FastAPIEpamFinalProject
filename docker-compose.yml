services:
  pg:
    image: postgres:18
    container_name: pg_db
    env_file:
      - app/.env
    ports:
      - "5432:5432"
    volumes:
      - pg_data_storage:/var/lib/postgresql

volumes:
  pg_data_storage:
